{
  "name": "@jsonstore/cli",
  "version": "0.1.0",
  "description": "Command-line interface for JSON Store",
  "type": "module",
  "bin": {
    "jsonstore": "./dist/cli.js"
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist *.tsbuildinfo",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "keywords": ["json", "database", "cli", "git"],
  "license": "MIT",
  "dependencies": {
    "@jsonstore/sdk": "workspace:*",
    "commander": "^12.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "typescript": "^5.3.3",
    "vitest": "^1.2.0"
  }
}
